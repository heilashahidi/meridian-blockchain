# Rust build artifacts
target/
**/*.rs.bk
Cargo.lock.bak

# Anchor / Solana (added by U1; pre-staged here so U2 build artifacts don't leak)
.anchor/
test-ledger/

# Node (frontend / tooling, future workstreams)
node_modules/

# Env
.env
.env.local

# OS / editor noise
.DS_Store
*.swp
